#!/usr/bin/make -f

%:
	dh $@ --buildsystem=ruby --with ruby

# Force xz compression so the package is installable on Debian in case we're
# building on an Ubuntu host.
override_dh_builddeb:
	dh_builddeb -- -Zxz
